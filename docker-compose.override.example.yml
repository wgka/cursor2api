services:
  cursor2api: {}
    # 复制为 docker-compose.override.yml 后，docker compose 会自动叠加这些本地配置
    # cp docker-compose.override.example.yml docker-compose.override.yml

    # 示例 1：为服务器单独指定构建镜像源（无需改 Dockerfile）
    # build:
    #   args:
    #     NPM_REGISTRY: https://registry.npmmirror.com

    # 示例 2：为服务器单独追加环境变量（无需改 docker-compose.yml）
    # environment:
    #   PROXY: http://host.docker.internal:7890
    #   AUTH_TOKEN: sk-your-secret-token
    #   CURSOR_MODEL: anthropic/claude-sonnet-4.6

    # 示例 3：自定义端口映射 / 额外挂载
    # ports:
    #   - "3010:3010"
    # volumes:
    #   - ./extra-data:/app/extra-data
